{
  "name": "eventhub-playwright-framework",
  "version": "1.0.0",
  "description": "Production-ready Playwright TypeScript framework adhering to strict POM, API seeding, and fixture isolation principles.",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:dev": "cross-env TEST_ENV=dev npx playwright test",
    "test:qa": "cross-env TEST_ENV=qa npx playwright test",
    "test:prod": "cross-env TEST_ENV=prod npx playwright test",
    "report:html": "npx playwright show-report",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.19.43",
    "allure-playwright": "^2.13.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "pino": "^8.19.0",
    "pino-pretty": "^10.3.1",
    "typescript": "^5.3.3"
  }
}
